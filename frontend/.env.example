# Used by Vite in local development. Docker production builds use /api/v1.
VITE_API_URL=http://localhost:8000/api/v1
# Optional: change the Vite proxy destination for containerised development.
VITE_DEV_API_TARGET=http://localhost:8000
